# Generated by canal-web. Do not edit manually.
server:
  port: 18083
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
canal.conf:
  mode: tcp
  flatMessage: true
  zookeeperHosts:
  syncBatchSize: 1000
  retries: -1
  timeout:
  accessKey:
  secretKey:
  consumerProperties:
    canal.tcp.server.host: 127.0.0.1:11111
    canal.tcp.zookeeper.hosts:
    canal.tcp.batch.size: 500
    canal.tcp.username:
    canal.tcp.password:
  srcDataSources:
    verify_mysql_source:
      url: jdbc:mysql://localhost:3306/canal_sync_verify?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: 12345678

  canalAdapters:
  - instance: verify_mysql_dest
    groups:
    - groupId: g1
      outerAdapters:
      - name: rdb
        key: verify_mysql_target
        properties:
          jdbc.driverClassName: com.mysql.jdbc.Driver
          jdbc.url: "jdbc:mysql://localhost:3306/canal_sync_target?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true"
          jdbc.username: "root"
          jdbc.password: "12345678"
          threads: 1
          commitSize: 3000
          batchSize: 3000
          scheduleTime: 600
      - name: rdb
        key: verify_pgsql_target
        properties:
          jdbc.driverClassName: org.postgresql.Driver
          jdbc.url: "jdbc:postgresql://127.0.0.1:5432/postgres?gssEncMode=disable&sslmode=disable&loginTimeout=60&connectTimeout=30&socketTimeout=60"
          jdbc.username: "postgres"
          jdbc.password: "123456"
          threads: 1
          commitSize: 3000
          batchSize: 3000
          scheduleTime: 600
      - name: es8
        key: verify_es_target
        hosts: http://127.0.0.1:9200
        properties:
          mode: rest
          cluster.name: my-elasticsearch
          security.auth: 
      - name: custom-redis
        key: verify_redis_target
        properties:
          destination: 
          groupId: g1
          sourceDatabase: 
          sourceTable: user_profile
          host: 127.0.0.1
          port: 6379
          password: ""
          database: 6
          keyPattern: "user_profile:{id}"
          valueType: HASH
          ttlSeconds: 0
          deletePolicy: DELETE_KEY
          mappingFile: "../conf/custom-redis/verifyredis000000000001.yml"


